// Usamos esta clase para tener uan serie de constantes para no tener que usar
// directamente números en el código de otros ficheros.js, con lo que quedará más claro
export class EstadosPartida {
    static ATACANTE = 1;
    static DEFENSOR = 2;
    static EMPATE = 0;
}